/**
 * Encryption module 
 * if user is authorized then encrypt the content and write to the file system
 */

module.exports = Encrypt;

/**
 * Encrypt the config object
 */
function Encrypt() {
    /**
     * Encryption Logic
     */
}